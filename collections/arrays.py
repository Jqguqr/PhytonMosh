# Arrays is used when we  have perform problems,
# because Lists are collections more complex for this
# rason is harder to handle than Arrays
from array import array

number = array("i", [1, 2, 3])
print(number)
